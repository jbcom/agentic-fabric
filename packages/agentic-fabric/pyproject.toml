[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agentic-fabric"
version = "1.3.0"
description = "Framework-agnostic agent orchestration with A2A, MCP, runtime adapters, and vendor-fabric tools"
requires-python = ">=3.11"
license = { text = "MIT" }
readme = "README.md"
authors = [{ name = "Jon Bogaty", email = "jon@jonbogaty.com" }]
maintainers = [{ name = "Jon Bogaty", email = "jon@jonbogaty.com" }]
keywords = ["a2a", "mcp", "crewai", "langgraph", "strands", "ai-agents", "multi-agent", "orchestration", "vendor-fabric"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Typing :: Typed",
]
dependencies = [
    "pyyaml>=6.0.3",
    "vendor-fabric>=2.1.5,<3.0.0",
]

[project.optional-dependencies]
a2a = ["a2a-sdk[http-server]>=1.1.2,<2.0.0", "uvicorn>=0.35.0"]
langgraph = ["langgraph>=0.2.0", "langchain-anthropic>=0.3.0", "langchain-ollama>=0.3.0"]
strands = ["strands-agents[ollama]>=0.1.0"]
mcp = ["jsonschema>=4.26.0", "mcp>=1.23.0,<2.0.0"]
scraping = ["beautifulsoup4>=4.12.0", "requests>=2.31.0"]
anthropic = ["vendor-fabric[anthropic]>=2.1.5,<3.0.0"]
aws = ["vendor-fabric[aws]>=2.1.5,<3.0.0"]
cursor = ["vendor-fabric[cursor]>=2.1.5,<3.0.0"]
github = ["vendor-fabric[github]>=2.1.5,<3.0.0"]
google = ["vendor-fabric[google]>=2.1.5,<3.0.0"]
meshy = ["vendor-fabric[meshy]>=2.1.5,<3.0.0"]
secrets-sync = ["vendor-fabric[secrets-sync]>=2.1.5,<3.0.0"]
slack = ["vendor-fabric[slack]>=2.1.5,<3.0.0"]
vault = ["vendor-fabric[vault]>=2.1.5,<3.0.0"]
zoom = ["vendor-fabric[zoom]>=2.1.5,<3.0.0"]
tests = [
    "coverage[toml]>=7.14.3",
    "jsonschema>=4.26.0",
    "pytest-agentic-fabric[tests]",
    "pytest-asyncio>=1.4.0",
    "pytest-cov>=7.1.0",
    "pytest-mock>=3.15.1",
    "pytest>=9.1.1",
    "pytest-timeout>=2.4.0",
    "pytest-xdist>=3.8.0",
]
typing = [
    "mypy>=2.1.0",
    "types-PyYAML>=6.0.12.20260518",
    "types-requests>=2.33.0.20260518",
]
dev = [
    "agentic-fabric[tests,typing]",
    "ruff>=0.16.0",
]
docs = []

[project.scripts]
agentic-fabric = "agentic_fabric.main:main"
agentic-fabric-meshy-mcp = "agentic_fabric.tools.meshy_mcp:main"
agentic-fabric-vendor-mcp = "agentic_fabric.tools.vendor_mcp:main"
agentic-fabric-vendor-a2a = "agentic_fabric.a2a:main"

[project.urls]
Documentation = "https://jonbogaty.com/agentic-fabric/"
Issues = "https://github.com/jbcom/agentic-fabric/issues"
Source = "https://github.com/jbcom/agentic-fabric"
Changelog = "https://github.com/jbcom/agentic-fabric/blob/main/CHANGELOG.md"

[tool.hatch.build.targets.wheel]
packages = ["src/agentic_fabric"]

[tool.pytest.ini_options]
addopts = ["-ra", "--strict-markers", "--strict-config"]
testpaths = ["tests"]
xfail_strict = true
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
timeout = 30
timeout_method = "thread"
markers = [
    "crewai: tests that require CrewAI runtime dependencies",
    "e2e: end-to-end tests that require external runtime dependencies",
    "langgraph: tests that require LangGraph runtime dependencies",
    "strands: tests that require Strands runtime dependencies",
    "vendor_fabric: tests that require vendor-fabric runtime dependencies",
]

[tool.coverage.run]
branch = true
parallel = true
source = ["agentic_fabric"]

[tool.coverage.report]
show_missing = true
skip_covered = true

[tool.ruff]
target-version = "py311"
line-length = 120
src = ["src", "tests", "examples"]

[tool.ruff.lint]
select = ["ALL"]
ignore = [
    "ANN",
    "ARG",
    "BLE001",
    "C901",
    "COM",
    "CPY",
    "D105",
    "D205",
    "D415",
    "D417",
    "DTZ007",
    "E402",
    "E501",
    "EM",
    "ERA001",
    "FBT",
    "FIX",
    "G",
    "PERF",
    "PLC0415",
    "PLR0124",
    "PLR0911",
    "PLR0912",
    "PLR0913",
    "PLR0915",
    "PLR0917",
    "PLR2004",
    "PLW0603",
    "PTH",
    "RET505",
    "RUF012",
    "S105",
    "S110",
    "S310",
    "S603",
    "SLF001",
    "TCH",
    "TD",
    "TID252",
    "TRY003",
    "TRY300",
    "TRY301",
    "TRY401",
]

[tool.ruff.lint.per-file-ignores]
"examples/**" = ["T201"]
"tests/**" = ["B018", "BLE", "D", "DTZ", "INP001", "LOG001", "N802", "N803", "N806", "PLC0415", "PT", "S", "SIM117", "TRY"]
"src/agentic_fabric/main.py" = ["T201"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.isort]
lines-between-types = 1
lines-after-imports = 2

[tool.mypy]
python_version = "3.11"
pretty = true
show_error_codes = true
ignore_missing_imports = true
warn_return_any = false
warn_unused_configs = true
exclude = ["tests/e2e/.*", "tests/.*/conftest.py"]

[[tool.mypy.overrides]]
module = [
    "bs4",
    "crewai",
    "crewai.*",
    "langchain_anthropic",
    "langgraph",
    "langgraph.*",
    "strands",
]
ignore_missing_imports = true
